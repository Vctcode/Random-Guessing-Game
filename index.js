let rangeMax = 2;
let userName = prompt("what is your name?")

function guesser(range=2) {
    let randomNum = Math.floor(Math.random() * range) + 1;
  
  while(level < 10) {
    
  }
}

console.log("ade")